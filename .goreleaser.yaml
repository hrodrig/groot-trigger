version: 2

project_name: groot-trigger

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: groot-trigger
    main: ./cmd/groot-trigger
    binary: groot-trigger
    flags:
      - -trimpath
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - freebsd
      - openbsd
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.branch={{.Branch}}
      - -X main.buildDate={{.Date}}

archives:
  - id: groot-trigger-dist
    ids:
      - groot-trigger
    formats: [tar.gz]
    name_template: "groot-trigger_{{ .Tag }}_{{ .Os }}_{{ .Arch }}"
    files:
      - src: LICENSE
        dst: share/doc/groot-trigger/LICENSE
      - src: README.md
        dst: share/doc/groot-trigger/README.md
      - src: contrib/man/man1/groot-trigger.1
        dst: share/man/man1/groot-trigger.1

checksum:
  name_template: checksums.txt

# Cosign keyless signing (GitHub Actions OIDC). Requires id-token: write in release.yml.
signs:
  - cmd: cosign
    env:
      - COSIGN_EXPERIMENTAL=1
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"
      - --yes
    artifacts: checksum

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^\\.github:"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

sboms:
  - id: groot-trigger-sbom-spdx
    artifacts: source
    documents:
      - "groot-trigger_{{ .Version }}_sbom.spdx.json"
    args: ["$artifact", "--output", "spdx-json=$document"]
  - id: groot-trigger-sbom-cdx
    artifacts: source
    documents:
      - "groot-trigger_{{ .Version }}_sbom.cyclonedx.json"
    args: ["$artifact", "--output", "cyclonedx-json=$document"]

dockers_v2:
  - dockerfile: Dockerfile.release
    sbom: true
    ids:
      - groot-trigger
    images:
      - ghcr.io/hrodrig/groot-trigger
    tags:
      - "v{{ .Version }}"
      - "{{ if not .IsSnapshot }}latest{{ end }}"
    labels:
      org.opencontainers.image.title: "groot-trigger"
      org.opencontainers.image.description: "On-demand HTTP API that creates Kubernetes Jobs running groot collect"
      org.opencontainers.image.source: "https://github.com/hrodrig/groot-trigger"
      org.opencontainers.image.version: "{{ .Version }}"
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.created: "{{ .Date }}"

docker_signs:
  - cmd: cosign
    env:
      - COSIGN_EXPERIMENTAL=1
    args:
      - sign
      - "${artifact}@${digest}"
      - --yes
